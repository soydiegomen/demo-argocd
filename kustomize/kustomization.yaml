apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

# Permite cambiar la versión/tag de la imagen fácilmente
images:
  - name: nginxdemos/hello
    newTag: "plain-text"
